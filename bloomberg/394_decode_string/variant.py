# — Title: Decode Compressed FIX Message
# Difficulty: Medium
# Prompt: Given an encoded trade message string where patterns like “3[AB]2[C]” represent repeated fields (e.g., for FIX-style compression), decode it into its expanded form. Nested encodings are possible and must be fully expanded.